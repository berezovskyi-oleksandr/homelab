apiVersion: v1
kind: Service
metadata:
  name: immich-db
  namespace: immich
  labels:
    app: immich-db
spec:
  type: ClusterIP
  ports:
    - name: 5432-5432
      port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: immich-db
