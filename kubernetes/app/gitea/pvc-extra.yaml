apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitea-extra
  namespace: gitea
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-synology-ssd
  resources:
    requests:
      storage: 30Gi
